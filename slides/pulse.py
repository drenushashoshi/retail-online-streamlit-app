"""Slide 1 — Business pulse: how did we do and what's ahead?

Owner: P3 — TODO, in progress.
MUST: monthly revenue line + MoM % + YoY % with st.metric(delta=...).
BONUS: two-year overlay by month + automatic seasonality note.
"""
from __future__ import annotations

import pandas as pd


def slide_pulse(df_sales: pd.DataFrame) -> None:
    """Render Slide 1 in Streamlit."""
    raise NotImplementedError("P3 — in progress")
